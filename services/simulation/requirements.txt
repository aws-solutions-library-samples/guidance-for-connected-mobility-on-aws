# Pinned 2026-05-29 to exact versions from the prod working simulator image
# `route-length-20260505-091153` (digest sha256:57c4bf2ad9c721…0ed88).
# Captured via `pip3 list` in a one-shot ECS task on cms-prod-fwe-simulator:10.
# Container base: python:3.13-slim → Python 3.13.13, Debian 13.4.
#
# Per ~/.kiro/steering/dependency-versions.md: direct deps + transitives
# pinned to exact versions for reproducible builds. Bumping any version is
# a separate decision (security patches: upgrade immediately; routine bumps:
# 7-day quarantine + verification run).

# --- Direct deps (originally listed, now pinned) ---
boto3==1.43.2
awsiotsdk==1.29.0
paho-mqtt==2.1.0
flask==3.1.3
flask-cors==6.0.2
requests==2.33.1
cantools==41.3.1
python-can==4.6.1
can-isotp==2.0.7
msgpack==1.1.2

# --- Server (also installed by Dockerfile's `pip install ... gunicorn`) ---
gunicorn==25.3.0

# --- Transitive deps (pinned for reproducible resolution) ---
argparse-addons==0.12.0
awscrt==0.32.1
bitstruct==8.22.1
blinker==1.9.0
botocore==1.43.2
certifi==2026.4.22
charset-normalizer==3.4.7
click==8.3.3
crccheck==1.3.1
diskcache==5.6.3
idna==3.15
itsdangerous==2.2.0
Jinja2==3.1.6
jmespath==1.1.0
MarkupSafe==3.0.3
packaging==26.2
python-dateutil==2.9.0.post0
s3transfer==0.17.0
six==1.17.0
textparser==0.24.0
typing_extensions==4.15.0
urllib3==2.6.3
Werkzeug==3.1.8
wrapt==1.17.3
